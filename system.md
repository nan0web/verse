---
title: "system.md — інструкція для verse"
description: "Інструкція для LLM і розробників, щоб створювати код у відповідності з принципами НаМір і nan0coding.architect"
PROJECT_NAME: "verse"
VERSION: "1.0.0"
CORE_PRINCIPLE: "Кожен атом коду — це акт відповідальності. Немає зайвих ідей, лише дія."
IDENTITY_MODEL: "Я / тИ / мИ / вИ"
LOGIC_BASE: "Атом → Молекула → Організм → Всесвіт"
ETIMOLOGY: "verse = версія світу, записана в коді"
---

# 🌌 `verse` — версія світу в коді

> _"Ти не програмуєш, ти **створюєш факти**"_  
> — вИ (LLM або розробник)

---

## 🎯 Чому це існує?

У світі — багато даних, але **немає довіри до структури**.

`verse` — це довірена система типів і моделей, щоб:
- Створювати **чіткі факти про світ** (контакти, імена, статі, групи)
- Не залежати від TypeScript, але **мати типи в голові**
- Писати код, який **можна тестувати, зрозуміти, змінити**

Кожна модель — це **відповідь на питання**:  
_"Що це дійсно означає?"_

---

## ⚙️ Принципи `verse`

### 1. Модель = Інформація + Структура + Дія

Кожна модель:
- Повертається через `from()`
- Має `toString()` та `toObject()`
- Перевіряється `fromString()` або через конструктор
- Покрита тестом: `*.test.js`

> Завдання: **як легко, швидко і зрозуміло — подати факт?**

### 2. Java•Script як мова знання

- Не просто код — **автодокументований об'єкт**
- Кожна змінна має тип у `JSDoc`
- Всі `import / export` — явні
- Немає стрілкових функцій без імені
- Немає обчислень наперед — **все через дію**

---

## 🧮 Іменування

| Тип | Приклад | Назва |
|-----|---------|--------|
| Клас | `HumanName` | `UpperCamelCase` |
| Метод | `toString()` | `camelCase` |
| Статичний метод | `parse()`, `from()` | `camelCase` |
| Змінна | `name`, `gender` | `camelCase` |
| Імпорт | `{ HumanName }` | явний |

---

## 🧱 Клас як атом

Кожен клас:
- має `input` або `value`
- має строгу логіку конструктора
- не змінюється після створення
- має `from(value)` як **точку входу**

> ```js
> class I {
>   /** @type {string} */
>   value
>   constructor(value) {
>     this.value = String(value)
>   }
>   toString() {
>     return this.value
>   }
>   static from(value) {
>     if (value instanceof I) return value
>     return new I(value)
>   }
> }
> ```

Клас `I` — **мінімальний факт**.

---

## 🏗 Проєктні стандарти: неМір → намІр

| Критерій | НеМІР ❌ | НаМІР ✅ |
|--------|-----------|----------|
| Код | Не тестується | 100% покритий тестами |
| Імпорт | Довільний, глибокий, без імені | Явний (`import { I } from '../I.js'`) |
| UI | Ілюзія доступності | Факт відповідальності |
| Типи | `any`, `undefined` | `@type`, явна структура |
| Залежності | Надмірність | Мінімум (`@nan0web/*`) |
| Використання | Примус | Автономія, вибір |

---

## 🗂 Структура проєкту

```yaml
/packages/verse/
├── src/
│   ├── Verse/
│   │   ├── Human/
│   │   │   ├── Contact.js
│   │   │   ├── Gender.js
│   │   │   ├── Name.js
│   │   │   └── index.js
│   │   ├── I/
│   │   └── We/
│   ├── README.md.js       ← документація як тест
│   └── index.js

├── types/                 ← JS -> TS автоматично
├── .datasets/             ← для LLM
├── playground/main.js     ← живий приклад
├── package.json           ← ключові скрипти
├── tsconfig.json
└── CHANGELOG.md
```

---

## 🛠 Команди

| Команда | Призначення |
|--------|-------------|
| `pnpm test` | Всі unit-тести |
| `pnpm test:docs` | Генерує README.md з тестів |
| `pnpm test:coverage` | Перевірка покриття |
| `pnpm test:status` | Статус пакету |
| `pnpm test:release` | Перевірка активного релізу |
| `pnpm build` | Генерує `.d.ts` типи |
| `pnpm playground` | Живий приклад |

---

## 🔁 Створення: тИ → вИ

Коли тИ починаєш модель, запитай:
- _"Що я створюю?"_
- _"Як це буде виглядати як об’єкт?"_
- _"Чи можна обійтись без генераторів?"_

вИ допомагає:
- зберегти чіткість
- не вибігати за абстракціями
- тестувати **через приклади**

> _"Все лишнє — це НеМІР."_

---

## 🧪 Тестування: факти замість уяви

Всі приклади у `README.md.js` — **реальні тести**:

```js
it("How to create HumanContact from string?", () => {
  //import { HumanContact } from '@nan0web/verse'
  const contact = HumanContact.from("+1234567890")
  console.info(String(contact)) // ← tel:+1234567890
  assert.equal(console.output()[0][1], "tel:+1234567890")
})
```

> Увага: тест перевіряє не `логіку`, а `результат`.
> Його можна **повторити, зрозуміти і довірити**.

---

## 🧬 Документація як тест

`src/README.md.js` використовується:
- для тестування (`npm test:docs`)
- для генерації `README.md`
- для побудови `.dataset` для навчання LLM

> ```js
> /**
> * @docs
> * ## API
> * ### HumanName.firstName → string
> * ```
> it("API: HumanName exposes firstName", () => {/* тут тест */})

---

## 🧠 LLM + розробник: один вимір

> _"Ти пишеш не для себе. Ти пишеш для — **вИ**."_  

LLM може:
- зрозуміти клас
- використати в тестах
- викликати API через `playground`

Розробник може:
- прочитати `README.md.js`
- виконати `pnpm playground`
- побачити `package status`

---

> _"Кожен об’єкт — як істина: він не повторюється. Він розширюється."_  

— вИ
